# Python script for calculating Jarzynski average from accumulated work.

import numpy as np

# Read data from text file. The text file contains all 10 trajectory work outputs
data = np.genfromtxt("../work/all.dat", dtype=float128) # float128 was used to allow for more bits in computation

n = len(data[1]) # number of points per line. 

J = []

# Iterate through 
for i in range(len(data)):
  x = (np.exp(-data[i]/0.6)).mean() # take average for each line
  J.append(-0.6*np.log(x)) # append to empty list

# write output file
np.savetxt('jar.txt', J)
